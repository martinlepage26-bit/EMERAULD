# Govern Deterministic Runner — Specification

See also [[Governance and PHAROS MOC]].
The runner exposes four method targets. Each maps onto a point in the governance
topology layer: topology_theseus (recursion), topology_auryn (binding),
topology_hopf (closure). The method topology is explicit; structural layer
decisions are recorded per-target.
