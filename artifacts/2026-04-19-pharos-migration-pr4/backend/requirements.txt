# PHAROS Backend — Python dependencies
# Entry point: uvicorn server:app (see Dockerfile)
#
# ⚠️  DO NOT REMOVE dnspython.
#     Motor requires it for mongodb+srv:// (Atlas) connections.
#     Without it, AsyncIOMotorClient raises ConfigurationError
#     at instantiation and every DB-backed endpoint fails.
#     This was the P1 bug in PR #4. Do not repeat it.

# ── Core framework ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Database ──
motor==3.6.0
pymongo==4.9.2
dnspython==2.6.1          # REQUIRED for mongodb+srv:// — see warning above

# ── Utilities ──
python-dotenv==1.0.1
python-multipart==0.0.12
resend==2.5.0

# ── Testing ──
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
